Count 함수 : 갯수를 세는 함수(Null Value는 세지 않는다)
Ex)
select count(*)
from customers

Ex)
select distinct count(*)
from products

Sum / Avg : 자료의 합과 평균 값을 구한다.
 * Avg는 Null 값을 무시한다(집계에 포함시키지 않는다)
 
Min / Max : 자료의 최소값과 최대값을 구한다.
